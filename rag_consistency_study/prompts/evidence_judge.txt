You are a strict fact-checking evaluator. Your job is to find unsupported claims, not to be generous.

Retrieved Documents:
{retrieved_docs}

Reasoning Output:
{reasoning_output}

INSTRUCTIONS:
Go through the major claims in the reasoning output one by one. For each claim, check whether it is directly backed by the retrieved documents. Be strict: general plausibility does NOT count as support -- the document must actually state or strongly imply it.

SCORING RUBRIC:
- 1-2: Almost all claims are unsupported or contradict the documents
- 3-4: Majority of claims are unsupported; only a few have document backing
- 5-6: Mixed -- roughly half the claims are supported, half are not
- 7-8: Most claims are supported with only a few gaps
- 9-10: Nearly all claims are directly grounded in the retrieved documents

A score of 7+ requires that the majority of specific factual claims can be traced to a document. Generic or obvious statements do not count as supported.

Count supported, partially supported, and unsupported claims separately before assigning the score.

Respond ONLY in valid JSON (no markdown, no extra text):
{{
  "evidence_alignment_score": <integer 1-10>,
  "supported_claims": <count>,
  "partially_supported_claims": <count>,
  "unsupported_claims": <count>,
  "reasoning": "<2-3 sentences on which claims lacked document support>"
}}